<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Plain HTTP is only allowed for the dev backend (10.0.2.2 = localhost
         from the Android emulator) and the prod server IP, until it gets a
         domain + TLS (see DEPLOY.md "Adding TLS later" in axiom-backend). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">2.24.138.223</domain>
    </domain-config>
</network-security-config>
